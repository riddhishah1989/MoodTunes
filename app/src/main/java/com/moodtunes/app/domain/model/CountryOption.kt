package com.moodtunes.app.domain.model

data class CountryOption(
    val name: String,   // shown to user — "Malaysia"
    val code: String,   // sent to API — "MY"
    val flag: String,   // shown in dropdown — "🇲🇾"

)

